---
import ThemeIcon from './ThemeIcon.astro';
import {CircleUser, Home, Rss, ScrollText, SquareDashedBottomCode} from 'lucide-react';
import {AstroNavMenu} from './AstroNavMenu';
import "@/styles/globals.css"

const items = [
	{
		title: 'Home',
		url: '/',
		icon: Home,
	},
	{
		title: 'Projects',
		url: '/projects',
		icon: SquareDashedBottomCode,
	},
	{
		title: 'Resume',
		url: '/resume',
		icon: ScrollText,
	},
	{
		title: 'Blog',
		url: '/blog',
		icon: Rss,
	},
	{
		title: 'About',
		url: '/about',
		icon: CircleUser,
	},
];
---

<div class='flex gap-16 md:gap-10'>
	<a href='/' class='hidden items-center space-x-2 md:flex'>
		<span class='hidden font-bold sm:inline-block'>Alex Westerman</span>
	</a>
	<AstroNavMenu items={items} />
	<!-- <Button
		variant='ghost'
		class='ml-4 flex items-center space-x-2 md:hidden'
		onClick={() => setShowMobileMenu(!showMobileMenu)}>
		{showMobileMenu ? <X /> : <Menu />}
		<span class='font-bold'>Alex Westerman</span>
	</Button> -->
	<!-- {showMobileMenu && <MobileNav closeFn={setShowMobileMenu}>{children}</MobileNav>} -->
	<span class='grow'></span>
	<div class='flex pr-2'>
		<ThemeIcon />
	</div>
</div>
